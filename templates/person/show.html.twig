{% extends 'base.html.twig' %}

{% block title %}Description de l'interlocuteur {{ person.name }} {{ person.forname }} {% endblock %}


{% block body %}
	<h1>Description</h1>
	
	<br><br>
    
    <table class="table table-hover">
        <tbody>
            <tr>
              <th>Nom</th>
                <td>{{ person.name }}</td>
            </tr>
            <tr>
              <th>Prénom</th>
              	<td>{{ person.forname }}</td>
            </tr>
            <tr>
              <th>Sexe</th>
                <td>{{ person.gender }}</td>
            </tr>
            <tr>
              <th>Statut</th>
                <td>{{ person.job }}</td>
            </tr>
        </tbody>
    </table>
	
	<a class="btn btn-success" role="button" href="{{ path('person_edit', {'id' : person.id }) }}">Modifier cet interlocuteur</a>
{% endblock %}

